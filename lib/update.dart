import 'package:flutter/cupertino.dart';
class UpdatePage extends StatelessWidget {
   var value;
  UpdatePage({Key key, this.value}) : super(key: key);

  static String tag = 'calculator-page';
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}