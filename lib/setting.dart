import 'package:flutter/cupertino.dart';
class SettingPage extends StatelessWidget {
   var value;
  SettingPage({Key key, this.value}) : super(key: key);

  static String tag = 'calculator-page';
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}